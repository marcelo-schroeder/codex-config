interface:
  display_name: "my-automation-email"
  short_description: "Read and recover configured Gmail access"
  default_prompt: "Use $my-automation-email to search my Gmail accounts for relevant messages."

policy:
  allow_implicit_invocation: false
